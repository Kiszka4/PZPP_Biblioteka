<Window x:Class="PZPP_Biblioteka.KsiążkaSzczegółyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PZPP_Biblioteka"
        mc:Ignorable="d"
        Title="Szczegóły Książki" Height="380" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Background="#f0f7f4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2e7d5e" Padding="15,10">
            <TextBlock Text="📖 Szczegóły książki" Foreground="White"
                       FontSize="15" FontWeight="Bold"/>
        </Border>

        <StackPanel Grid.Row="1" Margin="20,15">
            <TextBlock Text="{Binding Tytuł}" FontSize="20"
                       FontWeight="Bold" Foreground="#1a3d2b" Margin="0,0,0,12"/>

            <Border Background="White" CornerRadius="8" Padding="15,12"
                    BorderBrush="#d0e8db" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Autor:"
                               Foreground="#7a7974" FontSize="13" Margin="0,4"/>
                    <TextBlock Grid.Row="0" Grid.Column="1"
                               Text="{Binding Autor.PełneNazwisko}"
                               Foreground="#1a3d2b" FontWeight="SemiBold" FontSize="13" Margin="0,4"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="ISBN:"
                               Foreground="#7a7974" FontSize="13" Margin="0,4"/>
                    <TextBlock Grid.Row="1" Grid.Column="1"
                               Text="{Binding ISBN}"
                               Foreground="#1a3d2b" FontSize="13" Margin="0,4"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Gatunek:"
                               Foreground="#7a7974" FontSize="13" Margin="0,4"/>
                    <TextBlock Grid.Row="2" Grid.Column="1"
                               Text="{Binding GatunekKsiążki.Nazwa}"
                               Foreground="#1a3d2b" FontSize="13" Margin="0,4"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Ilość na stanie:"
                               Foreground="#7a7974" FontSize="13" Margin="0,4"/>
                    <TextBlock Grid.Row="3" Grid.Column="1"
                               Text="{Binding IloscNaStanie}"
                               Foreground="#1a3d2b" FontWeight="Bold" FontSize="13" Margin="0,4"/>
                </Grid>
            </Border>
        </StackPanel>
    </Grid>
</Window>