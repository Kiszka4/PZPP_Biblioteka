<Window x:Class="PZPP_Biblioteka.FiliaEdytujWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edytuj Filię" Height="220" Width="380"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15" Background="#f0f7f4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="110"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Edytuj filię"
                   FontSize="15" FontWeight="Bold" Margin="0,0,0,12"
                   Foreground="#1a5c42"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Nazwa:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1"
                 Text="{Binding Nazwa, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,3" Padding="5,4"/>

        <Label Grid.Row="2" Grid.Column="0" Content="Lokalizacja:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1"
                 Text="{Binding Lokalizacja, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,3" Padding="5,4"/>

        <Button Grid.Row="4" Grid.ColumnSpan="2" Content="Zapisz zmiany"
                Command="{Binding ZapiszCommand}"
                HorizontalAlignment="Right" Width="120" Height="30"
                Background="#2e7d5e" Foreground="White"
                FontWeight="Bold" BorderThickness="0" Margin="0,10,0,0"/>
    </Grid>
</Window>